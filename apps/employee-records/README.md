# employee-records

The Next.js application for **FrogsAtWorkHR**. This is one package in a monorepo — see the
[root README](../../README.md) for the project overview, architecture, and full setup
instructions.

```bash
# From the repo root, after the one-time setup in the root README:
cd apps/employee-records && pnpm dev   # → http://localhost:3000
```
